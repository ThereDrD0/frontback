# Практическая работа 15

Простое приложение для заметок с `HTTPS`, `Service Worker` и архитектурой `App Shell`.

## Запуск

```bash
cd client
npm install
mkcert -install
mkcert localhost 127.0.0.1 ::1
npm start
```

Адрес: `https://localhost:3001`
